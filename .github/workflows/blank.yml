# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  push:
  
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Create GitHub Variable
        run: |
          "secretdata='$((ConvertFrom-Json ${{ env.secret }}).a)'" | Out-File -Append -FilePath $env:GITHUB_ENV
          "justsecret='${{ env.secret }}'" | Out-File -Append -FilePath $env:GITHUB_ENV
          "data=Some Data" | Out-File -Append -FilePath $env:GITHUB_ENV
        shell: pwsh
        env:
          secret: ${{ secrets.JSON }}

      - name: Echo env:data
        run: |
          Write-Output "Out: ${{ env.data }}"
          Write-Host "Host: ${{ env.outsecret }}"
        shell: pwsh
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Echo env:data
        run: |
          Write-Output "Out: ${{ env.data }}"
          Write-Host "Host: ${{ env.outsecret }}"
        shell: pwsh

